# Telegram lead delivery
TG_BOT_TOKEN=
TG_CHAT_ID=

# Meta Conversions API (SECRET — never commit/log/echo)
META_CAPI_TOKEN=

# Meta Pixel id (public by design)
NEXT_PUBLIC_META_PIXEL_ID=

# Optional: allowed origin override (defaults to vercel subdomain + localhost)
# ALLOWED_ORIGIN=protocol-face-landing.vercel.app